// Lucky 21: Task 1 — old Hit-or-Bust simulate tests removed.
// simulateMachine and simulateSession for blackjack-reel are re-implemented
// in later tasks once the Lucky 21 engine is complete.

import { describe, it } from 'vitest'

describe('lucky-21 simulate — later task', () => {
  it.todo('exactRtp dispatch for blackjack-reel — later task')
  it.todo('simulateMachine convergence — later task')
  it.todo('simulateSession smoke — later task')
})
