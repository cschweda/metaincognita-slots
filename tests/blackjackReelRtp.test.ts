// Lucky 21: Task 1 — old Hit-or-Bust RTP tests removed.
// The DP solver (optimalAction, decisionEvs, strategyMatrixCell, blackjackReelExactRtp)
// is being replaced for the Lucky 21 stop-the-reels mechanic.
// Tests are restored in later tasks as the new engine is implemented.

import { describe, it } from 'vitest'

describe('lucky-21 RTP — later task', () => {
  it.todo('blackjackReelExactRtp implemented in a later task')
  it.todo('optimalStop implemented in a later task')
  it.todo('decisionEvs implemented in a later task')
  it.todo('strategyMatrixCell implemented in a later task')
})
