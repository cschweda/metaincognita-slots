// Lucky 21: Task 1 — Hit-or-Bust machine-integrity tests removed.
// HIT_OR_BUST uses the old def shape and is no longer on the FLOOR.
// Tests are restored in a later task once the Lucky 21 machine def is complete.

import { describe, it } from 'vitest'

describe('lucky-21 machine definition — later task', () => {
  it.todo('passes validateMachineDef without errors — later task')
  it.todo('frozen calibration — later task')
  it.todo('integer-money invariant — later task')
})
